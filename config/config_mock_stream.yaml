version: 1
metadata:
  experiment_name: mock_stream
  description: "Mock streaming benchmark run"
  seed: 321
servers:
  - name: mock_server
    type: mock
    base_url: https://mock.local
    model: mock-model
scenarios:
  - name: short_chat
    type: chat_short
    prompts_file: data/prompts/short.jsonl
    runs: 6
    concurrency: [1, 2]
    request:
      max_output_tokens: 32
      temperature: 0.1
      stream: true
quality:
  perplexity:
    enabled: false
  qa_accuracy:
    enabled: false
metrics: {}
report:
  formats: [markdown, json]
  output_dir: results/mock_stream
storage:
  backend: json
  path: results/mock_stream/raw
warmup:
  requests: 0
retry_policy:
  strategy: exponential_backoff
  base_delay_ms: 100
  max_attempts: 2
cost_model:
  enabled: false
remote_metrics: []

